# Typesense Configuration
# Copy to .env if you need to override defaults

# Defaults are already configured in client.py - only set if you need to change
# TYPESENSE_HOST=localhost
# TYPESENSE_PORT=8108
# TYPESENSE_API_KEY=govbrnews_api_key_change_in_production
